#include <iostream>

#include "parser/basic_parser.h"

int main()
{
    std::cout << "Hello world!" << '\n';
    // regex::parser::basic_parser parser;
    // parser.compute("(abc)+d|ef");
    return 0;
}